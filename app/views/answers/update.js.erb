$('.answer-errors').html(`<%= render 'shared/form_errors', resource: @answer %>`)

<% if !@answer.errors.present? %>
  $('.answers').html(`<%= j render @question.answers.without_best(@question.best_answer) %>`)
<% end %>