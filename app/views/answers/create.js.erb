$('.answer-errors').html(`<%= render 'shared/form_errors', resource: @answer %>`)

<% if @answer.persisted? %>
  $('.answers').append(`<%= j render @answer %>`)
  $('.new-answer #answer_body').val('')
<% end %>